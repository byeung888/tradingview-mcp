services:
  - type: web
    name: tv-webhook-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn tv_webhook_server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: WHATSAPP_NUMBER
        sync: false
      - key: CALLMEBOT_API_KEY
        sync: false
